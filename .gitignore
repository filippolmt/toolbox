# GSD planning artifacts (internal workflow, not for public repo)
.planning/

# Go binary
toolbox

# Per-developer toolbox config override
.toolbox.yaml

# GoReleaser
dist/

# Claude Code local settings
.claude/settings.local.json
.claude/worktrees/

# macOS
.DS_Store

# graphify: commit the rest of graphify-out/ so the team shares the map
# (per https://github.com/safishamsi/graphify#team-setup)
graphify-out/cache/
graphify-out/manifest.json
graphify-out/cost.json
graphify-out/.graphify_*
# Dated rebuild snapshots: per-day mirror of top-level files, regenerated by the
# branch-switch hook. Top-level GRAPH_REPORT.md/graph.html/graph.json stay tracked
# as the canonical team-shared map.
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]*/
docs/superpowers/specs/
docs/superpowers/plans/

# >>> sdd-managed/openspec (toolbox)
.claude/skills/openspec-*/
.claude/commands/opsx/
.codex/skills/openspec-*/
openspec/config.yaml
# <<< sdd-managed/openspec (toolbox)
